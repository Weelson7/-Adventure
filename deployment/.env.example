# Default environment variables for Docker Compose
# Copy this file to .env and customize as needed

# Server Configuration
ADVENTURE_PORT=8080
STAGING_PORT=8081

# Java Options (heap size, GC settings, etc.)
JAVA_OPTS=-Xmx2g -Xms512m

# Application Mode (server, client, interactive)
ADVENTURE_MODE=server
